function [A, B, c] = compute_warp(S, T, varargin)
% X are initial points
% Y are cooresponding points we would like to match to
     lambda = 1;
     if ~isempty(varargin)
         lambda = varargin{1};
     end
	 [d,n] = size(S);
	 %%YOUR CODE HERE
end
